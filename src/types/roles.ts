/** User roles — matches database CHECK constraint. */
export const USER_ROLES=["SUPER_ADMIN","ADMIN","MODERATOR","VERIFIED_STUDENT","STUDENT","SUSPENDED","BANNED"] as const;export type UserRole=(typeof USER_ROLES)[number];
// Trusted owner identity for server-side authorization fallbacks.
export const SUPER_ADMIN_USER_ID="a22a60b0-8010-47a7-8626-1b59433aefaf";
export const ADMIN_ROLES:readonly UserRole[]=["SUPER_ADMIN","ADMIN","MODERATOR"] as const;export const INVISIBLE_ROLES:readonly UserRole[]=["SUPER_ADMIN","ADMIN"] as const;export const ACTIVE_STUDENT_ROLES:readonly UserRole[]=["VERIFIED_STUDENT","STUDENT"] as const;export const RESTRICTED_ROLES:readonly UserRole[]=["SUSPENDED","BANNED"] as const;
const ADMIN_ROLE_SET=new Set<UserRole>(ADMIN_ROLES),INVISIBLE_ROLE_SET=new Set<UserRole>(INVISIBLE_ROLES),ACTIVE_STUDENT_ROLE_SET=new Set<UserRole>(ACTIVE_STUDENT_ROLES),RESTRICTED_ROLE_SET=new Set<UserRole>(RESTRICTED_ROLES);
export const isAdminRole=(role?:string|null):role is "SUPER_ADMIN"|"ADMIN"|"MODERATOR"=>Boolean(role&&ADMIN_ROLE_SET.has(role as UserRole));export const isInvisibleRole=(role?:string|null):role is "SUPER_ADMIN"|"ADMIN"=>Boolean(role&&INVISIBLE_ROLE_SET.has(role as UserRole));export const isActiveStudent=(role?:string|null):role is "VERIFIED_STUDENT"|"STUDENT"=>Boolean(role&&ACTIVE_STUDENT_ROLE_SET.has(role as UserRole));export const isRestrictedRole=(role?:string|null):role is "SUSPENDED"|"BANNED"=>Boolean(role&&RESTRICTED_ROLE_SET.has(role as UserRole));export const isVerifiedStudent=(role?:string|null):role is "VERIFIED_STUDENT"=>role==="VERIFIED_STUDENT";export const isSuperAdmin=(role?:string|null):role is "SUPER_ADMIN"=>role==="SUPER_ADMIN";export const isSuperAdminUser=(userId?:string|null)=>userId===SUPER_ADMIN_USER_ID;
