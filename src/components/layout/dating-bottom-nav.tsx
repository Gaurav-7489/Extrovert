"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {Compass,Flame,Heart,MessageCircle,UserRound,Settings} from "lucide-react";import {cn} from "@/lib/utils";import {routes} from "@/config/routes";
const items=[{href:routes.discover,label:"Discover",icon:Flame},{href:routes.social,label:"Explore",icon:Compass},{href:routes.likes,label:"Likes",icon:Heart},{href:routes.messages,label:"Chat",icon:MessageCircle},{href:routes.profile,label:"Profile",icon:UserRound}];
export function DatingBottomNav(){const pathname=usePathname();const hide=pathname===routes.onboarding||pathname.startsWith(routes.login)||pathname.startsWith(routes.register);const inChatThread=pathname.startsWith(`${routes.messages}/`);if(hide)return null;return <><header className="shrink-0 border-b border-[#eee5e2] bg-[#fffdfb]/95 backdrop-blur-xl md:hidden"><div className="mx-auto flex h-12 max-w-md items-center justify-between px-4"><Link href={routes.discover} aria-label="Extrovert home" className="flex items-center gap-1.5 text-[18px] font-black tracking-tight text-zinc-950"><span className="grid h-7 w-7 place-items-center overflow-hidden rounded-[9px] bg-[#761f30]"><img src="/extrovert-mark.svg" alt="" className="h-full w-full"/></span>extrovert</Link>{!inChatThread&&<Link href={routes.settings} aria-label="Settings" className="grid h-8 w-8 place-items-center rounded-full border border-zinc-200 bg-white text-zinc-700 shadow-sm active:scale-95"><Settings className="h-4 w-4"/></Link>}</div></header>{!inChatThread&&<nav aria-label="Extrovert navigation" className="fixed inset-x-0 bottom-0 z-[80] border-t border-[#eee5e2] bg-[#fffdfb]/95 pb-[env(safe-area-inset-bottom)] backdrop-blur-xl md:hidden"><div className="mx-auto grid h-[68px] max-w-md grid-cols-5 px-1">{items.map(item=>{const Icon=item.icon;const active=pathname===item.href||pathname.startsWith(`${item.href}/`);return <Link key={item.href} href={item.href} className={cn("flex min-w-0 flex-col items-center justify-center gap-1 text-[10px] font-semibold transition-transform active:scale-90",active?"text-[#761f30]":"text-zinc-500")}><Icon className={cn("h-[24px] w-[24px]",active&&item.href===routes.discover&&"fill-current")} strokeWidth={active?2.5:1.9}/><span>{item.label}</span></Link>})}</div></nav>}</>}
