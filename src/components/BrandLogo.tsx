import Image from "next/image";
export default function BrandLogo({size=36,showText=true,className=""}:{size?:number;showText?:boolean;className?:string}){return <div className={`flex items-center gap-2.5 select-none ${className}`}><div className="relative grid shrink-0 place-items-center overflow-hidden rounded-[28%] bg-[rgb(var(--brand))] shadow-[0_4px_16px_rgb(var(--brand)/.24)] ring-1 ring-white/10" style={{width:size,height:size}}><Image src="/icon-192.png" alt="Extrovert" fill className="object-cover" priority sizes={`${size}px`}/></div>{showText&&<span className="flex items-center font-black tracking-[-0.035em] text-[rgb(var(--text-primary))]" style={{fontSize:Math.max(18,size*.52)}}>Extrovert<span className="ml-0.5 inline-block h-1.5 w-1.5 rounded-full bg-[rgb(var(--accent))]"/></span>}</div>}
