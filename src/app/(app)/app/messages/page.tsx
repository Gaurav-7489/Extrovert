import type { Metadata } from "next";import Link from "next/link";import Image from "next/image";import { EmptyState } from "@/components/shared/empty-state";import { routes } from "@/config/routes";import { getProfilePhotoUrl } from "@/lib/profile-photo";import { createServerSupabaseClient } from "@/lib/supabase/server";import { MessageSquare, Flame, HeartHandshake, LockKeyhole, Search, Users, ShieldCheck } from "lucide-react";import SuperChatRequestCard from "@/components/messages/superchat-request-card";
export const metadata: Metadata = { title: "Chat | Extrovert" };export const dynamic = "force-dynamic";
type Match={id:string;user_a:string;user_b:string;created_at:string};type DateMessage={id:string;match_id:string;sender_id:string;content:string|null;ciphertext:string|null;encryption_version:number;created_at:string};type DateProfile={id:string;display_name:string|null;department:string|null;academic_year:string|null;date_of_birth:string|null;profile_photos:Array<{storage_path:string;display_order:number;is_primary:boolean}>|null};type SocialConversation={id:string;connection_id:string;created_at:string};type SocialConnection={id:string;requester_id:string;target_id:string;status:string};type SocialProfile={id:string;display_name:string|null;department:string|null;profile_photo_path:string|null;verification_status:string|null};type SocialMessage={id:string;conversation_id:string;sender_id:string;ciphertext:string;created_at:string};type Request={id:string;sender_id:string;content:string;created_at:string;display_name:string;department:string;academic_year:string;profile_photos:{storage_path:string;is_primary:boolean;display_order:number}[]};type Chat={key:string;href:string;name:string;photoUrl:string|null;latest:string;createdAt:string;mine:boolean;kind:"Social"|"Dating";verified:boolean};
export default async function MessagesPage(){const supabase=await createServerSupabaseClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return null;const[{data:rawMatches,error:matchesError},{data:requestRows},{data:memberRows}]=await Promise.all([supabase.from("matches").select("id,user_a,user_b,created_at").or(`user_a.eq.${user.id},user_b.eq.${user.id}`).order("created_at",{ascending:false}),supabase.rpc("get_superchat_requests"),supabase.from("extrovert_conversation_members").select("conversation_id").eq("user_id",user.id)]);const requests=(requestRows??[]) as Request[];const matches=(rawMatches??[]) as Match[];const matchIds=matches.map(m=>m.id);const socialConversationIds=Array.from(new Set((memberRows??[]).map((r:{conversation_id:string})=>r.conversation_id)));const[dateMessagesRes,dateProfilesRes,socialConversationsRes]=await Promise.all([matchIds.length?supabase.from("messages").select("id,match_id,sender_id,content,ciphertext,encryption_version,created_at").in("match_id",matchIds).order("created_at",{ascending:false}):Promise.resolve({data:[],error:null}),matchIds.length?supabase.rpc("get_match_profiles",{p_user_ids:Array.from(new Set(matches.map(m=>m.user_a===user.id?m.user_b:m.user_a)))}):Promise.resolve({data:[],error:null}),socialConversationIds.length?supabase.from("extrovert_conversations").select("id,connection_id,created_at").in("id",socialConversationIds):Promise.resolve({data:[],error:null})]);const dateMessages=(dateMessagesRes.data??[]) as DateMessage[];const dateProfiles=(dateProfilesRes.data??[]) as DateProfile[];const dateProfileMap=new Map(dateProfiles.map(p=>[p.id,p]));const latestDate=new Map<string,DateMessage>();for(const message of dateMessages)if(!latestDate.has(message.match_id))latestDate.set(message.match_id,message);const socialConversations=(socialConversationsRes.data??[]) as SocialConversation[];const connectionIds=socialConversations.map(c=>c.connection_id);const[{data:connections},{data:socialMessages}]=await Promise.all([connectionIds.length?supabase.from("extrovert_connections").select("id,requester_id,target_id,status").in("id",connectionIds).eq("status","accepted"):Promise.resolve({data:[],error:null}),socialConversationIds.length?supabase.from("extrovert_messages").select("id,conversation_id,sender_id,ciphertext,created_at").in("conversation_id",socialConversationIds).order("created_at",{ascending:false}):Promise.resolve({data:[],error:null})]);const socialConnections=(connections??[]) as SocialConnection[];const socialOtherIds=Array.from(new Set(socialConnections.map(c=>c.requester_id===user.id?c.target_id:c.requester_id)));const{data:socialProfiles}=socialOtherIds.length?await supabase.from("extrovert_profiles").select("id,display_name,department,profile_photo_path,verification_status").in("id",socialOtherIds):{data:[]};const socialProfileMap=new Map((socialProfiles??[]).map(p=>[p.id,p as SocialProfile]));const latestSocial=new Map<string,SocialMessage>();for(const message of(socialMessages??[]) as SocialMessage[])if(!latestSocial.has(message.conversation_id))latestSocial.set(message.conversation_id,message);const datingChats:Chat[]=[];const newMatches:{matchId:string;profile:DateProfile;photoUrl:string|null}[]=[];for(const match of matches){const other=match.user_a===user.id?match.user_b:match.user_a;const profile=dateProfileMap.get(other);if(!profile)continue;const photos=[...(profile.profile_photos??[])].sort((a,b)=>Number(b.is_primary)-Number(a.is_primary)||a.display_order-b.display_order);const photoUrl=getProfilePhotoUrl(photos[0]?.storage_path,160);const latest=latestDate.get(match.id);if(!latest)newMatches.push({matchId:match.id,profile,photoUrl});else datingChats.push({key:`dating-${match.id}`,href:`${routes.messages}/${match.id}`,name:profile.display_name??"Match",photoUrl,latest:latest.encryption_version===1?"Encrypted message":latest.content??"Message",createdAt:latest.created_at,mine:latest.sender_id===user.id,kind:"Dating",verified:true})}const socialChats:Chat[]=[];for(const conversation of socialConversations){const connection=socialConnections.find(c=>c.id===conversation.connection_id);if(!connection)continue;const other=connection.requester_id===user.id?connection.target_id:connection.requester_id;const profile=socialProfileMap.get(other);if(!profile)continue;const latest=latestSocial.get(conversation.id);if(!latest)continue;socialChats.push({key:`social-${conversation.id}`,href:`${routes.messages}/social/${conversation.id}`,name:profile.display_name??"Connection",photoUrl:getProfilePhotoUrl(profile.profile_photo_path,160),latest:"Encrypted message",createdAt:latest.created_at,mine:latest.sender_id===user.id,kind:"Social",verified:profile.verification_status==="verified"})}const chats=[...datingChats,...socialChats].sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime());return <main className="mx-auto w-full max-w-md px-3 pb-24 pt-3 font-sans text-zinc-950 md:max-w-lg md:px-4 md:pt-4"><header className="px-1"><div className="flex items-center justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.18em] text-emerald-600">Your connections</p><h1 className="text-3xl font-black tracking-tight">Chat</h1></div><Link href={routes.social} aria-label="Explore" className="grid h-10 w-10 place-items-center rounded-full border border-zinc-200 bg-white text-zinc-700"><Users className="h-5 w-5"/></Link></div><div className="mt-3 flex h-11 items-center gap-2 rounded-full border border-zinc-200 bg-zinc-50 px-4"><Search className="h-4 w-4 text-zinc-400"/><span className="text-sm text-zinc-400">Search matches</span></div></header>{newMatches.length>0&&<section className="mt-4"><div className="mb-3 flex items-center gap-1.5 px-1"><Flame className="h-4 w-4 fill-current text-emerald-600"/><h2 className="text-xs font-black uppercase tracking-wider text-zinc-700">New matches</h2></div><div className="flex gap-3 overflow-x-auto pb-2">{newMatches.map(m=><Link key={m.matchId} href={`${routes.messages}/${m.matchId}`} className="group w-20 shrink-0 text-center"><div className="relative mx-auto h-[68px] w-[68px] overflow-hidden rounded-full border-2 border-emerald-500 p-0.5 bg-zinc-100">{m.photoUrl?<Image src={m.photoUrl} alt="" fill sizes="68px" className="rounded-full object-cover"/>:<div className="grid h-full place-items-center rounded-full bg-emerald-50 font-black text-emerald-700">{m.profile.display_name?.charAt(0)}</div>}</div><span className="mt-1 block truncate text-[10px] font-bold text-zinc-700">{m.profile.display_name?.split(" ")[0]}</span></Link>)}</div></section>}{requests.length>0&&<section className="mt-4"><div className="mb-2 px-1 text-xs font-black uppercase tracking-wider text-amber-700">Message requests ({requests.length})</div><div className="space-y-2">{requests.map(r=><SuperChatRequestCard key={r.id} requestId={r.id} senderName={r.display_name} content={r.content}/>)}</div></section>}{matchesError?<div className="mt-5"><EmptyState icon={<MessageSquare className="h-6 w-6"/>} title="Couldn&apos;t load messages" description="Please try again in a moment."/></div>:chats.length===0&&newMatches.length===0&&requests.length===0?<div className="mt-6"><EmptyState icon={<HeartHandshake className="h-6 w-6"/>} title="No conversations yet" description="Your matches and social connections will appear here."/></div>:<section className="mt-5"><div className="mb-2 flex items-center gap-1 px-1 text-xs font-black uppercase tracking-wider text-zinc-500"><MessageSquare className="h-3.5 w-3.5"/>Messages</div><div className="space-y-1">{chats.map(chat=><ConversationItem key={chat.key} chat={chat}/>)}</div></section>}{chats.length>0&&<div className="mt-4 flex items-center justify-center gap-1 text-[9px] font-semibold text-zinc-400"><LockKeyhole className="h-3 w-3"/>Protected conversations</div>}</main>}
function ConversationItem({chat}:{chat:Chat}){return <Link href={chat.href} className="flex items-center gap-3 rounded-2xl border border-transparent px-2.5 py-3 transition active:bg-zinc-50 hover:border-zinc-100 hover:bg-zinc-50"><div className="relative h-14 w-14 shrink-0 overflow-hidden rounded-full bg-zinc-100">{chat.photoUrl?<Image src={chat.photoUrl} alt="" fill sizes="56px" className="object-cover"/>:<div className="grid h-full place-items-center text-sm font-black text-emerald-600">{chat.name.charAt(0)}</div>}</div><div className="min-w-0 flex-1"><div className="flex items-center gap-1.5"><p className="truncate text-sm font-black text-zinc-950">{chat.name}</p>{chat.verified&&<ShieldCheck className="h-3.5 w-3.5 text-emerald-600"/>}<span className="rounded-full bg-zinc-100 px-1.5 py-0.5 text-[8px] font-bold text-zinc-500">{chat.kind}</span></div><p className="truncate text-xs text-zinc-500">{chat.mine?"You: ":""}{chat.latest}</p></div><time className="shrink-0 text-[9px] font-medium text-zinc-400">{formatRelative(chat.createdAt)}</time></Link>}
function formatRelative(value:string){const diff=Math.max(0,Date.now()-new Date(value).getTime());const minutes=Math.floor(diff/60000);if(minutes<1)return"now";if(minutes<60)return`${minutes}m`;const hours=Math.floor(minutes/60);if(hours<24)return`${hours}h`;const days=Math.floor(hours/24);return days<7?`${days}d`:new Date(value).toLocaleDateString([], {day:"numeric",month:"short"});}
