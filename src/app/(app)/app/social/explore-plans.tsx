"use client";

import { useState } from "react";
import { CalendarDays, Clock3, MapPin, Plus, Users, X, Crown, MessageCircle, Check, ChevronRight } from "lucide-react";
import { Button } from "@/components/ui/button";

type Plan = {
  id:string; emoji:string; title:string; area:string; date:string; time:string; members:number; capacity:number; coordinator:string; description:string; category:string;
};

const starterPlans:Plan[]=[
 {id:"coffee",emoji:"☕",title:"Sunset Coffee",area:"Shimla",date:"Sat, 12 Sep",time:"6:30 PM",members:3,capacity:6,coordinator:"Gaurav",description:"Coffee, conversation and a relaxed evening.",category:"Chill"},
 {id:"hike",emoji:"🏔️",title:"Sunday Hike",area:"Wakanaghat",date:"Sun, 13 Sep",time:"8:00 AM",members:5,capacity:8,coordinator:"Priya",description:"Easy morning hike. Bring water and good shoes.",category:"Adventure"},
 {id:"movie",emoji:"🎬",title:"Movie Night",area:"Solan",date:"Sat, 12 Sep",time:"7:45 PM",members:2,capacity:6,coordinator:"Rahul",description:"Movie and food afterwards. Requests are reviewed.",category:"Movies"},
 {id:"roadtrip",emoji:"🚗",title:"Weekend Road Trip",area:"Shimla",date:"19–21 Sep",time:"7:00 AM",members:4,capacity:6,coordinator:"Ankit",description:"Small group road trip. Final itinerary in the lobby chat.",category:"Travel"},
];

const categories=["All","Chill","Food","Movies","Adventure","Travel","Music"];

export default function ExplorePlans(){
 const [plans,setPlans]=useState(starterPlans); const [category,setCategory]=useState("All"); const [selected,setSelected]=useState<Plan|null>(null); const [creating,setCreating]=useState(false); const [joined,setJoined]=useState<string[]>([]);
 const visible=category==="All"?plans:plans.filter(p=>p.category===category);
 function createPlan(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const plan:Plan={id:crypto.randomUUID(),emoji:String(f.get("emoji")||"✨"),title:String(f.get("title")||"New Plan"),area:String(f.get("area")||"Shimla"),date:String(f.get("date")||"Date to be set"),time:String(f.get("time")||"Time to be set"),members:1,capacity:Math.max(2,Math.min(20,Number(f.get("capacity"))||6)),coordinator:"You",description:String(f.get("description")||"Let’s make a plan."),category:String(f.get("category")||"Chill")};setPlans(p=>[plan,...p]);setCreating(false);setSelected(plan);}
 function joinPlan(id:string){setJoined(v=>v.includes(id)?v:v.concat(id));setPlans(v=>v.map(p=>p.id===id?{...p,members:Math.min(p.capacity,p.members+1)}:p));}
 return <main className="mx-auto w-full max-w-5xl px-3 pb-24 pt-4 font-sans sm:px-5">
   <header className="flex items-end justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-[.2em] text-emerald-600">Explore</p><h1 className="mt-1 text-3xl font-black tracking-tight">Find something to do.</h1><p className="mt-1 max-w-lg text-xs text-zinc-500">Join a plan, meet people, or create your own lobby.</p></div><Button size="md" leftIcon={<Plus className="h-4 w-4"/>} onClick={()=>setCreating(true)}>Create Plan</Button></header>
   <div className="mt-5 flex gap-2 overflow-x-auto pb-1">{categories.map(c=><button key={c} type="button" onClick={()=>setCategory(c)} className={`shrink-0 rounded-full border px-3.5 py-2 text-[10px] font-black transition ${category===c?"border-emerald-600 bg-emerald-600 text-white":"border-zinc-200 bg-white text-zinc-600 hover:border-emerald-200"}`}>{c}</button>)}</div>
   <section className="mt-6"><div className="mb-3 flex items-center justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.18em] text-emerald-600">Live lobbies</p><h2 className="text-lg font-black">Plans near you</h2></div><span className="rounded-full bg-zinc-100 px-2.5 py-1 text-[9px] font-black text-zinc-500">{visible.length} plans</span></div>
    <div className="grid gap-3 sm:grid-cols-2">{visible.map(plan=><button key={plan.id} type="button" onClick={()=>setSelected(plan)} className="group rounded-[1.7rem] border border-zinc-200 bg-white p-4 text-left shadow-sm transition hover:-translate-y-0.5 hover:border-emerald-200 hover:shadow-md"><div className="flex items-start gap-3"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-zinc-50 text-2xl">{plan.emoji}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><h3 className="truncate text-base font-black">{plan.title}</h3><span className="rounded-full bg-emerald-50 px-2 py-1 text-[8px] font-black text-emerald-700">OPEN</span></div><p className="mt-1 line-clamp-2 text-[11px] leading-4 text-zinc-500">{plan.description}</p></div></div><div className="mt-4 grid grid-cols-2 gap-2 text-[9px] font-bold text-zinc-500"><span><MapPin className="mr-1 inline h-3 w-3 text-emerald-600"/>{plan.area}</span><span><Clock3 className="mr-1 inline h-3 w-3 text-emerald-600"/>{plan.date} · {plan.time}</span></div><div className="mt-4 flex items-center justify-between border-t border-zinc-100 pt-3"><span className="text-[9px] font-black text-zinc-500"><Users className="mr-1 inline h-3.5 w-3.5 text-emerald-600"/>{plan.members}/{plan.capacity} players · {plan.coordinator} is coordinator</span><ChevronRight className="h-4 w-4 text-zinc-300 transition group-hover:translate-x-0.5 group-hover:text-emerald-600"/></div></button>)}</div>
   </section>
   <section className="mt-7 rounded-[1.8rem] border border-emerald-100 bg-emerald-50/60 p-4 sm:p-5"><div className="flex items-start gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-white text-emerald-600 shadow-sm"><Crown className="h-5 w-5"/></div><div><h2 className="text-sm font-black">Your lobby, your rules.</h2><p className="mt-1 text-[10px] leading-4 text-emerald-900/65">Create a plan, choose the player limit, review join requests, and coordinate everyone from one shared lobby.</p></div></div></section>
   {selected&&<div className="fixed inset-0 z-[100] grid place-items-center bg-zinc-950/25 p-3 sm:p-5" onClick={()=>setSelected(null)}><div className="w-full max-w-lg rounded-[2rem] border border-white/70 bg-white p-5 shadow-2xl" onClick={e=>e.stopPropagation()}><div className="flex items-start gap-3"><div className="grid h-14 w-14 shrink-0 place-items-center rounded-2xl bg-zinc-50 text-3xl">{selected.emoji}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><div><p className="text-[9px] font-black uppercase tracking-[.18em] text-emerald-600">Plan lobby</p><h2 className="mt-1 text-xl font-black">{selected.title}</h2></div><button type="button" onClick={()=>setSelected(null)} className="grid h-9 w-9 place-items-center rounded-full bg-zinc-100"><X className="h-4 w-4"/></button></div><p className="mt-1 text-[10px] text-zinc-500">👑 {selected.coordinator} · Coordinator</p></div></div><div className="mt-5 grid grid-cols-2 gap-2"><div className="rounded-2xl bg-zinc-50 p-3"><MapPin className="h-4 w-4 text-emerald-600"/><p className="mt-1 text-[9px] font-black text-zinc-400">MEET AT</p><p className="text-xs font-black">{selected.area}</p></div><div className="rounded-2xl bg-zinc-50 p-3"><CalendarDays className="h-4 w-4 text-emerald-600"/><p className="mt-1 text-[9px] font-black text-zinc-400">WHEN</p><p className="text-xs font-black">{selected.date}</p><p className="text-[9px] font-bold text-zinc-500">{selected.time}</p></div></div><div className="mt-4 rounded-2xl border border-zinc-200 p-3"><div className="flex items-center justify-between"><span className="text-[9px] font-black uppercase tracking-wider text-zinc-400">Players</span><span className="text-xs font-black">{selected.members}/{selected.capacity}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-zinc-100"><div className="h-full rounded-full bg-emerald-500 transition-all" style={{width:`${Math.min(100,selected.members/selected.capacity*100)}%`}}/></div><div className="mt-3 flex -space-x-2">{Array.from({length:Math.min(selected.members,6)}).map((_,i)=><span key={i} className="grid h-8 w-8 place-items-center rounded-full border-2 border-white bg-zinc-100 text-[9px] font-black">{i===0?"👑":"•"}</span>)}</div></div><p className="mt-3 text-xs leading-5 text-zinc-500">{selected.description}</p><div className="mt-4 flex gap-2"><Button variant="outline" className="flex-1" onClick={()=>setSelected(null)} leftIcon={<MessageCircle className="h-4 w-4"/>}>Lobby Chat</Button><Button className="flex-1" disabled={joined.includes(selected.id)||selected.members>=selected.capacity} onClick={()=>joinPlan(selected.id)} leftIcon={joined.includes(selected.id)?<Check className="h-4 w-4"/>:<Users className="h-4 w-4"/>}>{joined.includes(selected.id)?"Request sent":selected.members>=selected.capacity?"Lobby full":"Request to join"}</Button></div><p className="mt-3 text-center text-[8px] font-bold text-zinc-400">The coordinator reviews join requests.</p></div></div>}
   {creating&&<div className="fixed inset-0 z-[101] grid place-items-center bg-zinc-950/25 p-3 sm:p-5" onClick={()=>setCreating(false)}><form onSubmit={createPlan} onClick={e=>e.stopPropagation()} className="w-full max-w-lg rounded-[2rem] bg-white p-5 shadow-2xl"><div className="flex items-center justify-between"><div><p className="text-[9px] font-black uppercase tracking-[.18em] text-emerald-600">Create lobby</p><h2 className="mt-1 text-xl font-black">Create a Plan</h2></div><button type="button" onClick={()=>setCreating(false)} className="grid h-9 w-9 place-items-center rounded-full bg-zinc-100"><X className="h-4 w-4"/></button></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="sm:col-span-2 text-[10px] font-black">Plan name<input name="title" required placeholder="Coffee after class" className="mt-1 h-11 w-full rounded-2xl border border-zinc-200 px-3 text-xs font-medium"/></label><label className="text-[10px] font-black">Emoji<input name="emoji" defaultValue="☕" maxLength={2} className="mt-1 h-11 w-full rounded-2xl border border-zinc-200 px-3 text-lg"/></label><label className="text-[10px] font-black">Category<select name="category" className="mt-1 h-11 w-full rounded-2xl border border-zinc-200 px-3 text-xs"><option>Chill</option><option>Food</option><option>Movies</option><option>Adventure</option><option>Travel</option><option>Music</option></select></label><label className="text-[10px] font-black">Area<input name="area" required placeholder="Shimla" className="mt-1 h-11 w-full rounded-2xl border border-zinc-200 px-3 text-xs"/></label><label className="text-[10px] font-black">Date<input name="date" required placeholder="Sat, 19 Sep" className="mt-1 h-11 w-full rounded-2xl border border-zinc-200 px-3 text-xs"/></label><label className="text-[10px] font-black">Time<input name="time" required placeholder="6:30 PM" className="mt-1 h-11 w-full rounded-2xl border border-zinc-200 px-3 text-xs"/></label><label className="text-[10px] font-black">Player limit<input name="capacity" type="number" min={2} max={20} defaultValue={6} className="mt-1 h-11 w-full rounded-2xl border border-zinc-200 px-3 text-xs"/></label><label className="sm:col-span-2 text-[10px] font-black">Description<textarea name="description" rows={3} placeholder="What are you planning?" className="mt-1 w-full resize-none rounded-2xl border border-zinc-200 p-3 text-xs"/></label></div><div className="mt-4 flex gap-2"><Button type="button" variant="outline" className="flex-1" onClick={()=>setCreating(false)}>Cancel</Button><Button type="submit" className="flex-1" leftIcon={<Plus className="h-4 w-4"/>}>Open Lobby</Button></div></form></div>}
 </main>;
}
