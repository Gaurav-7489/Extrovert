import type {Metadata} from "next";import {Compass,ShieldCheck} from "lucide-react";import {createServerSupabaseClient} from "@/lib/supabase/server";import {getProfilePhotoUrl} from "@/lib/profile-photo";import ExploreMap,{type ExploreMapArea,type ExploreMapPerson} from "./explore-map";
export const metadata:Metadata={title:"Explore | Extrovert"};export const dynamic="force-dynamic";
type SocialProfile={id:string;display_name:string|null;area_id:string|null;gender:string|null;verification_status:string|null;area_verification_status:string|null;profile_photo_path:string|null};type AreaStat={id:string;name:string};type Connection={id:string;requester_id:string;target_id:string;status:string};
export default async function SocialPage(){const supabase=await createServerSupabaseClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return null;const{data:areaRows}=await supabase.from("extrovert_areas").select("id,name").in("name",["Waknaghat","Solan","Shimla"]);const areas=(areaRows??[]) as AreaStat[];const ids=areas.map(a=>a.id);const areaName=new Map(areas.map(a=>[a.id,a.name]));const[{data:people},{data:stats},{data:connections}]=await Promise.all([ids.length?supabase.from("extrovert_profiles").select("id,display_name,area_id,gender,verification_status,area_verification_status,profile_photo_path").in("area_id",ids).neq("id",user.id).neq("trust_state","banned").order("verification_status",{ascending:false}).limit(60):Promise.resolve({data:[]}),ids.length?supabase.from("extrovert_profiles").select("id,area_id,verification_status").in("area_id",ids).neq("trust_state","banned"):Promise.resolve({data:[]}),supabase.from("extrovert_connections").select("id,requester_id,target_id,status").or(`requester_id.eq.${user.id},target_id.eq.${user.id}`)]);const all=(connections??[]) as Connection[];const cmap=new Map<string,Connection>();for(const c of all)cmap.set(c.requester_id===user.id?c.target_id:c.requester_id,c);const accepted=all.filter(c=>c.status==="accepted");const conversationIds=accepted.map(c=>c.id);const{data:conversationRows}=conversationIds.length?await supabase.from("extrovert_conversations").select("id,connection_id").in("connection_id",conversationIds):{data:[]};const conversations=new Map((conversationRows??[]).map((c:{id:string;connection_id:string})=>[c.connection_id,c.id]));const profiles=(people??[]) as SocialProfile[];const mapPeople:ExploreMapPerson[]=profiles.map(p=>{const c=cmap.get(p.id);return{id:p.id,display_name:p.display_name,profile_photo_path:getProfilePhotoUrl(p.profile_photo_path,96),area_name:areaName.get(p.area_id??"")??"Unknown area",gender:p.gender,verification_status:p.verification_status,area_verification_status:p.area_verification_status,connection_status:c?.status??null,conversation_id:c?.status==="accepted"?conversations.get(c.id)??null:null}});const mapAreas:ExploreMapArea[]=["Waknaghat","Solan","Shimla"].map(name=>{const id=areas.find(a=>a.name===name)?.id;const rows=((stats??[]) as {area_id:string;verification_status:string}[]).filter(p=>p.area_id===id);return{name,users:rows.length,verified:rows.filter(p=>p.verification_status==="verified").length}});return <main className="mx-auto w-full max-w-5xl px-2.5 pb-24 pt-1.5 font-sans sm:px-5"><div className="mb-2 flex items-center gap-2 px-1 text-[9px] font-bold text-zinc-400"><Compass className="h-3.5 w-3.5 text-emerald-600"/><span>Explore local people · <ShieldCheck className="inline h-3 w-3 text-emerald-600"/> verified members are marked</span></div><ExploreMap people={mapPeople} areas={mapAreas}/></main>}
