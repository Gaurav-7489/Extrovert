"use client";
import {useMemo,useState} from "react";import {MapPin,Sparkles} from "lucide-react";import DiscoverClient,{type DiscoverProfile} from "./discover-smooth";
type Props={profiles:DiscoverProfile[];isPro?:boolean;nearbyArea?:string|null};
export default function DiscoverMode({profiles,isPro=false,nearbyArea=null}:Props){const[mode,setMode]=useState<"for-you"|"nearby">("for-you");const nearbyProfiles=useMemo(()=>{const area=nearbyArea?.trim().toLowerCase();if(!area)return[];return profiles.filter(p=>p.area_verification_status==="verified"&&p.area_name?.trim().toLowerCase()===area)},[profiles,nearbyArea]);const visible=mode==="nearby"?nearbyProfiles:profiles;return <div className="mx-auto flex w-full max-w-md flex-col px-2.5 pt-2"><div className="mb-2 grid grid-cols-2 rounded-2xl border border-zinc-200 bg-zinc-100/80 p-1 shadow-sm dark:border-zinc-800 dark:bg-zinc-900" role="tablist" aria-label="Discover mode"><button type="button" role="tab" aria-selected={mode==="for-you"} onClick={()=>setMode("for-you")} className={`rounded-xl px-3 py-2.5 text-xs font-black ${mode==="for-you"?"bg-white text-zinc-950 shadow-sm dark:bg-zinc-800 dark:text-white":"text-zinc-500"}`}>For You</button><button type="button" role="tab" aria-selected={mode==="nearby"} onClick={()=>setMode("nearby")} className={`flex items-center justify-center gap-1.5 rounded-xl px-3 py-2.5 text-xs font-black ${mode==="nearby"?"bg-white text-red-600 shadow-sm dark:bg-zinc-800":"text-zinc-500"}`}><MapPin className="h-3.5 w-3.5"/>Nearby</button></div>{mode==="nearby"&&nearbyArea&&nearbyProfiles.length>0&&<div className="mb-2 flex items-center gap-2 rounded-2xl border border-red-100 bg-red-50/70 px-3 py-2 text-[10px] font-bold text-red-800 dark:border-red-950 dark:bg-red-950/30 dark:text-red-200"><MapPin className="h-3.5 w-3.5"/>People in {nearbyArea}<span className="ml-auto rounded-full bg-white px-2 py-0.5 text-[9px] font-black dark:bg-zinc-800">{nearbyProfiles.length}</span></div>}{mode==="nearby"&&nearbyProfiles.length===0?<main className="flex min-h-[calc(100dvh-190px)] items-center justify-center px-2 py-6"><section className="w-full rounded-[2rem] border border-red-100 bg-red-50/60 p-7 text-center shadow-sm dark:border-red-950 dark:bg-red-950/20"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-white text-red-600 shadow-sm dark:bg-zinc-900"><MapPin className="h-7 w-7"/></div><p className="mt-4 text-[10px] font-black uppercase tracking-[.18em] text-red-600">NEARBY</p><h1 className="mt-1 text-2xl font-black">No one nearby yet.</h1><p className="mt-2 text-xs leading-5 text-zinc-500">Try For You to discover more people. Nearby shows members in your verified area without exposing exact locations.</p><button type="button" onClick={()=>setMode("for-you")} className="mt-5 inline-flex items-center gap-2 rounded-2xl bg-red-600 px-5 py-3 text-xs font-black text-white shadow-lg shadow-red-200/30"><Sparkles className="h-4 w-4"/>Explore For You</button></section></main>:<DiscoverClient key={mode} profiles={visible} isPro={isPro}/>}</div>}
