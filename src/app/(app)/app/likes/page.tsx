import type {Metadata} from "next";import Link from "next/link";import Image from "next/image";import {redirect} from "next/navigation";import {Heart,Lock,ShieldCheck,Sparkles} from "lucide-react";import {createServerSupabaseClient} from "@/lib/supabase/server";import {routes} from "@/config/routes";import {getProfilePhotoUrl} from "@/lib/profile-photo";import {calculateAge} from "@/lib/utils";
type Liker={liker_id:string;liked_at:string;display_name:string;date_of_birth:string;department:string|null;academic_year:string|null;identity_type:string|null;institution_name:string|null;job_title:string|null;profile_photos:{storage_path:string;is_primary:boolean;display_order:number}[]};type CountDb={from:(table:"likes")=>{select:(columns:string,options?:{count?:"exact";head?:boolean})=>{eq:(column:string,value:string)=>Promise<{count:number|null}>}}};export const metadata:Metadata={title:"Likes | Extrovert"};export const dynamic="force-dynamic";
export default async function LikesPage(){const supabase=await createServerSupabaseClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect(routes.login);const[{data:identity},{data:subscription},{count}]=await Promise.all([supabase.from("extrovert_profiles").select("gender").eq("id",user.id).maybeSingle(),supabase.from("subscriptions").select("plan,status,current_period_end").eq("user_id",user.id).maybeSingle(),(supabase as unknown as CountDb).from("likes").select("id",{count:"exact",head:true}).eq("liked_id",user.id)]);const paid=subscription?.plan==="pro"&&["active","trialing"].includes(subscription.status)&&!!subscription.current_period_end&&new Date(subscription.current_period_end).getTime()>Date.now();const woman=(identity?.gender??"").toLowerCase()==="woman";const allowed=woman||paid;const total=count??0;if(!allowed)return <main className="mx-auto max-w-md px-3.5 pb-24 pt-3 font-sans text-zinc-950"><p className="text-[10px] font-black uppercase tracking-[.18em] text-emerald-600">Likes</p><h1 className="mt-1 text-3xl font-black">Who likes you</h1><section className="mt-4 rounded-[2rem] border border-emerald-100 bg-white p-6 text-center shadow-sm"><div className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-emerald-50"><Heart className="h-7 w-7 fill-current text-emerald-600"/></div><p className="mt-4 text-5xl font-black">{total}</p><p className="mt-1 text-xs font-bold text-zinc-500">people have liked you</p><div className="mx-auto mt-5 rounded-2xl bg-zinc-50 p-4"><Lock className="mx-auto h-4 w-4 text-emerald-600"/><p className="mt-2 text-sm font-black">See who they are</p><p className="mt-1 text-[10px] leading-4 text-zinc-500">Unlock your incoming likes with Extrovert Beyond.</p></div><Link href={routes.extrovert} className="mt-4 inline-flex items-center gap-2 rounded-full bg-emerald-600 px-6 py-3 text-xs font-black text-white"><Sparkles className="h-3.5 w-3.5"/>View Extrovert Beyond</Link></section></main>;const{data,error}=await supabase.rpc("get_people_who_liked_me",{p_limit:100});if(error)console.error("Failed to load incoming likes:",error);const likers=(data??[]) as Liker[];return <main className="mx-auto max-w-md px-3.5 pb-24 pt-3 font-sans text-zinc-950"><p className="text-[10px] font-black uppercase tracking-[.18em] text-emerald-600">Likes</p><h1 className="mt-1 text-3xl font-black">Who likes you</h1><p className="mt-1 text-xs text-zinc-500">People who already swiped right.</p><div className="mt-4 grid grid-cols-2 gap-3">{likers.map(liker=>{const photo=[...(liker.profile_photos??[])].sort((a,b)=>Number(b.is_primary)-Number(a.is_primary)||a.display_order-b.display_order)[0];const photoUrl=getProfilePhotoUrl(photo?.storage_path,240);const age=calculateAge(liker.date_of_birth);const context=liker.job_title||liker.department||liker.identity_type||liker.institution_name||"Extrovert member";return <Link key={liker.liker_id} href={`${routes.profileView}/${liker.liker_id}`} className="overflow-hidden rounded-[1.5rem] border border-zinc-200 bg-white shadow-sm"><div className="relative aspect-[4/5] bg-zinc-100">{photoUrl?<Image src={photoUrl} alt="" fill sizes="(max-width:640px) 45vw,220px" className="object-cover"/>:<div className="grid h-full place-items-center text-3xl font-black text-zinc-300">{liker.display_name?.charAt(0)??"?"}</div>}<div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/70 to-transparent p-3 pt-10 text-white"><div className="flex items-center gap-1"><p className="truncate text-sm font-black">{liker.display_name}{age!==null?`, ${age}`:""}</p><ShieldCheck className="h-3.5 w-3.5 shrink-0"/></div><p className="mt-0.5 truncate text-[9px] text-white/80">{context}</p></div></div><div className="p-2.5"><p className="text-[9px] font-black uppercase tracking-wider text-emerald-600">Liked you</p></div></Link>})}</div>{likers.length===0&&<div className="mt-6 rounded-[2rem] border border-dashed border-zinc-200 bg-white p-7 text-center"><Heart className="mx-auto h-6 w-6 text-zinc-300"/><p className="mt-3 text-sm font-black">No likes yet</p><p className="mt-1 text-xs text-zinc-500">Keep swiping. New likes will appear here.</p></div>}</main>}
